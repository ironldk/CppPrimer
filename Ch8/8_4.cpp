#include"stdafx.h"
#include <iostream>
#include<string>
#include<vector>
using std::vector;
using std::string;
using std::ifstream;
using std::cin;

// Write a function to open a file for input and read its contents
// into a vector of strings, storing each line as a separate element in the
// vector.
void func(const string &ifile, vector<string> &vec) {
	ifstream in(ifile);
	i
}

int main() {
	string ifile = "lena.jpg";
	vector<string> vec;
	func(ifile, vec);
	return 0;
}

